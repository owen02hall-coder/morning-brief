# Python
__pycache__/
*.pyc
.venv/
venv/

# Local secrets / env (NEVER commit keys)
.env
*.local.env

# Build → workflow handoffs (job-local, never published)
headline.txt
audio.mp3

# Assumption-test fingerprints are committed on purpose; nothing else here.
