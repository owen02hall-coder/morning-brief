# Python
__pycache__/
*.pyc
.venv/
venv/

# Local secrets / env (NEVER commit keys)
.env
*.local.env

# Build → workflow ready-push handoff (job-local, never published)
headline.txt

# Assumption-test fingerprints are committed on purpose; nothing else here.
